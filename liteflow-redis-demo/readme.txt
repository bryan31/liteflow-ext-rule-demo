1.把配置中的信息替换成你自己的链接信息
2.节点中存的规则文件为：
THEN(a,b,c);
3.直接运行启动类即可，会自动执行LiteFLowCommand类中的方法。
4.可自己增加组件，替换规则。
5.执行类中每2秒运行一次，为什么要循环，是为了方便你测试热刷新。目前配置为轮询模式1秒轮询一次，可直接在redis里改规则即可。会实时刷新规则，执行结果会变。
6.如果要更改为订阅模式需增加mode=sub配置项，在Redisson客户端修改对应的RMapCache数据。